#include <bits/stdc++.h>
#define rep(i,n) for(int (i)=0;(i)<(n);(i)++)
const bool debug=false;
#define DEBUG if(debug==true)
using namespace std;
typedef long long ll;
typedef unsigned long long ull;

int main(void) {
    int a,b;
    cin >> a,b;
    vector<int> va(a,0), vb(b,0);
    int vala=0,valb=0;
    rep(i,a){
        cin >> va[i];
    }
    rep(i,b){
        cin >> vb[i];
    }
    while(!(a==-1&&b==-1)){
    }

    return 0;
}
